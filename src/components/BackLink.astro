---
interface Props {
    href: string;
    label?: string;
}
const { href, label = "Back" } = Astro.props;
---

<a class="no-underline w-fit hover:text-sidebar-primary" href={href}>
    ← {label}
</a>
